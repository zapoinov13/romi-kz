Синхронизируй проект с GitHub: zapoinov13/romi-kz, ветка main (последний коммит).

Supabase project ref: rgttklitvvqsnlsakvzr

КРИТИЧНО — исправление RUNTIME_ERROR edge functions:

1) SQL Editor — выполни целиком scripts/lovable-greenapi-ingest.sql
   (функция greenapi_ingest — без неё CRM не получает WA-сообщения через Vercel).

2) SQL Editor — scripts/lovable-whatsapp-cabinet-bind.sql (если ещё не делали).

3) Edge Functions — redeploy с verify_jwt=false:
   - greenapi-proxy (исправлен: без _lib import, default webhook → Vercel)
   - greenapi-webhook (исправлен синтаксис InstanceConfig)

4) Green API webhook URL:
   https://romi-kz.vercel.app/api/wa-webhook
   incoming webhook = yes

5) Проверка:
   POST https://romi-kz.vercel.app/api/wa-webhook body {"typeWebhook":"test"}
   → {"ok":true,"skipped":"test"}

Publish → Update после Git sync.
